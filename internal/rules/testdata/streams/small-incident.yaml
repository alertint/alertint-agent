# A small two-alert incident matches no baseline correlation rule:
# the engine leaves template selection to the skill defaults.
name: small-incident-no-match
description: Two related alerts in one service stay on the default LLM path.
alerts:
  - labels: {alertname: DiskFull, cluster: prod, namespace: default, service: api, severity: critical}
    annotations: {summary: "disk at 96%"}
  - labels: {alertname: DiskAlmostFull, cluster: prod, namespace: default, service: api, severity: warning}
    offset_seconds: 30
expect: {}
